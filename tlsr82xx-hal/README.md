# tlsr82xx-hal

Hardware abstraction layer crate for Telink TLSR82xx chips.

This crate will depend on `tlsr82xx-pac` once the generated PAC surface is in place.

Use matching chip features to select the generated PAC:

- `chip-8258`
- `chip-8278`
- `chip-826x`
