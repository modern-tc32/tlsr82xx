# `buttons8258`

Minimal GPIO input example for TLSR8258 on the `TB-03F` board.

- `BUTTON1` (`PA7` / `SWS`) drives both `LED_Y` (`PB4`) and `LED_W` (`PB5`)

Build from `tlsr82xx/examples/buttons8258`:

```sh
make debug
```
